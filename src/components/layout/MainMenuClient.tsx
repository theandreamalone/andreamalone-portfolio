import useHoverEffects from "@/util/useHoverEffects";

export default function MainMenuInteractive() {
    useHoverEffects();

    // This component only handles the hover effects
    // All the actual menu structure is in the server component
    return null;
}
